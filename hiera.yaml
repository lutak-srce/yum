---
version: 5

defaults:
  datadir: 'data'
  data_hash: yaml_data

hierarchy:
  - name: 'Operating System Major Release'
    path: "%{facts.os.release.major}.yaml"
  - name: 'defaults'
    path: 'defaults.yaml'
