# Name: Atomic Rocket Turtle RPM Repository for CentOS / Red Hat Enterprise Linux 5 - 
# URL: http://www.atomicrocketturtle.com/
# Note: This isn't covered by ASL support. -Scott

# Untested, Unstable, known buggy, and incomplete packages. 
[atomic-bleeding]
name = CentOS / Red Hat Enterprise Linux $releasever - atomic - (Bleeding)
baseurl = http://www.atomicorp.com/channels/atomic-bleeding/centos/$releasever/$basearch/
enabled = 0
protect = 0
gpgkey = file:///etc/pki/rpm-gpg/RPM-GPG-KEY.art.txt
gpgcheck = 1
priority = <%= priority %>
<% if exclude.to_s != '' -%>
exclude = <%= exclude.join(' ') %>
<% end -%>
